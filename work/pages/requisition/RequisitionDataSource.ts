/**
 * RequisitionDataSource — Customer override passthrough.
 * Re-exports the product class unchanged.
 * Override here to customize column suppression, labels, API endpoint, etc.
 */
export { RequisitionDataSource } from "@/page-defs/requisition/RequisitionDataSource";

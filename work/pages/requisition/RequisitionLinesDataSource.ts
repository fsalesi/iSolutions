/**
 * RequisitionLinesDataSource — Customer override passthrough.
 * Re-exports the product class unchanged.
 * Override here to customize line-level column suppression, labels, etc.
 */
export { RequisitionLinesDataSource } from "@/page-defs/requisition/RequisitionLinesDataSource";
